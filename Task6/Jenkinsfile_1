pipeline {
  agent any 
  stages{
    stage("docker-ps-command"){
      steps{
        sh 'docker ps -a'
        withCredentials([
          usernamePassword(credentials:'github-credentials', usernameVariable: USER, passwordVariable: PWD)
        ]){
          echo "${USER} ${PWD}"
        }
      }
    }
  }
}
    
